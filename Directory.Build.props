<Project>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Authors>Lars Oste (Kroste)</Authors>
    <Company>Kroste</Company>
    <RepositoryUrl>https://github.com/Kroste/Checkmk-Plugin-VSphereBaseImage</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <!-- MinVer soll die vX.Y.Z-Tags erkennen. Ohne diesen Prefix ignoriert MinVer
         die Tags und liefert 0.0.0-alpha.0 -> Plugin meldet Version "0.0.0". -->
    <MinVerTagPrefix>v</MinVerTagPrefix>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MinVer" PrivateAssets="all" />
  </ItemGroup>

</Project>
